/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: { unoptimized: true },
  basePath: '/Dgbusiness',
  assetPrefix: '/Dgbusiness/',
};

export default nextConfig;
